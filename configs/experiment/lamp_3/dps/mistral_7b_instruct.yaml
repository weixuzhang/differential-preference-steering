# @package _global_
defaults:
  - override /model: mistral_7b_instruct
  - override /data: lamp_3
  - override /decoder: dps

# Override decoder configs for LaMP-3
decoder:
  configs:
    task: LaMP-3
    num_preference_heads: 40

model:
  configs:
    max_new_tokens: 128
