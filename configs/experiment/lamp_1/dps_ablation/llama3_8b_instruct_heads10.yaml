# @package _global_
defaults:
  - override /model: llama3_8b_instruct
  - override /data: lamp_1
  - override /decoder: dps

# DPS Ablation Study: 10 preference heads for LaMP-1
decoder:
  configs:
    task: LaMP-1
    num_preference_heads: 10
    post_softmax: True

model:
  configs:
    max_new_tokens: 32

# Ablation study identifier
ablation:
  study: num_preference_heads
  value: 10
