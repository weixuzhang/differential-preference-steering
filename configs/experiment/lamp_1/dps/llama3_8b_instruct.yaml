# @package _global_
defaults:
  - override /model: llama3_8b_instruct
  - override /data: lamp_1
  - override /decoder: dps

# Override decoder configs for LaMP-1
decoder:
  configs:
    task: LaMP-1
    num_preference_heads: 40

model:
  configs:
    max_new_tokens: 32

