# @package _global_
defaults:
  - override /model: llama3_8b_instruct
  - override /data: lamp_5
  - override /decoder: dps

# Override decoder configs for LaMP-5
decoder:
  configs:
    task: LaMP-5
    num_preference_heads: 40

model:
  configs:
    max_new_tokens: 32  # Paper titles are typically short

