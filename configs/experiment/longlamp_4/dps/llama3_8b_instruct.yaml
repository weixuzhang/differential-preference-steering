# @package _global_
defaults:
  - override /model: llama3_8b_instruct
  - override /data: longlamp_4
  - override /decoder: dps

# Override decoder configs for LongLaMP-4
decoder:
  configs:
    task: LongLaMP-4
    num_preference_heads: 40

model:
  configs:
    max_new_tokens: 256  # Longer generation for product review
