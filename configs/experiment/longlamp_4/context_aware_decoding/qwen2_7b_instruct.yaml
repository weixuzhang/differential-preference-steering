# @package _global_
defaults:
  - override /model: qwen2_7b_instruct
  - override /data: longlamp_4
  - override /decoder: context_aware_decoding

# Context-Aware Decoding for LongLaMP-4
decoder:
  configs:
    alpha: 0.5  # Contrastive weight
    post_softmax: True

model:
  configs:
    max_new_tokens: 256
