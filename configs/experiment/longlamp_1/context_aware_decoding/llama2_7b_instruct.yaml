# @package _global_
defaults:
  - override /model: llama2_7b_instruct
  - override /data: longlamp_1
  - override /decoder: context_aware_decoding

# Context-Aware Decoding for LongLaMP-1
decoder:
  configs:
    alpha: 0.5  # Contrastive weight
    post_softmax: True

model:
  configs:
    max_new_tokens: 256
