# @package _global_
defaults:
  - override /model: llama3_8b_instruct
  - override /data: longlamp_1
  - override /decoder: dps

# Override decoder configs for LongLaMP-1
decoder:
  configs:
    task: LongLaMP-1
    num_preference_heads: 40

model:
  configs:
    max_new_tokens: 256  # Longer generation for email completion
