# @package _global_
defaults:
  - override /model: llama2_7b_instruct
  - override /data: longlamp_3
  - override /decoder: dps

# Override decoder configs for LongLaMP-3
decoder:
  configs:
    task: LongLaMP-3
    num_preference_heads: 40

model:
  configs:
    max_new_tokens: 256
